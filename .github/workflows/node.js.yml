name: Deploy to Ubuntu Server

on:
  push:
    branches:
      - v.1.1

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: docker build -t konzerra/common:unistandard-webapp .

      - name: Push Docker image to Docker Hub
        run: docker push konzerra/common:unistandard-webapp

#      - name: SSH into Ubuntu Server
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.SERVER_HOST }}
#          username: ${{ secrets.SERVER_USERNAME }}
#          password: ${{ secrets.SERVER_PASSWORD }}
#          port: ${{ secrets.SERVER_PORT }}
#          script: |
#            docker stop webapp || true
#            docker rm webapp || true
#            docker pull konzerra/common:unistandard-webapp
#            docker run -d --name webapp -p 5000:5000 konzerra/common:unistandard-webapp
